name: ci

on: pull_request

jobs:
  test:
    runs-on: macos-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v3
      - name: run test
        uses: subosito/flutter-action@v2
      - run: flutter test
